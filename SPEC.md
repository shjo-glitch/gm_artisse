# 아띠제 음료 주문표 기능 명세서

## 개요

아띠제 음료 주문표는 회사 구성원의 음료 주문을 한 화면에서 수집하고 여러 접속자가 함께 확인하는 ChatGPT Sites 웹 앱이다. 정적 화면과 API는 Sites에서 제공하며 구조화된 데이터는 Sites D1에 영구 저장한다.

## 아키텍처

- 프론트엔드는 같은 출처의 `/api/*` 엔드포인트만 호출한다.
- Worker가 D1의 `config`, `drinks`, `orders` 테이블을 읽고 변경한다.
- D1 바인딩 이름은 `DB`다.
- 관리자 비밀번호는 Sites의 `ADMIN_PASSWORD` secret으로 유지하고 Worker에서 검증한다.
- 브라우저는 5초마다 주문과 설정을 재조회해 다른 사용자의 변경을 반영한다.
- 기존 Supabase 데이터는 최초 D1 마이그레이션에 초기 데이터로 포함한다.

## 데이터 모델

- `config`: 단일 행. 팀원 목록, 옵션 목록, 주문 마감 상태를 JSON 문자열과 정수 플래그로 저장한다.
- `drinks`: 카테고리, 메뉴명, Tall/Grande 가격, ICE 전용 여부, 정렬 순서를 저장한다.
- `orders`: 주문 식별자, 메뉴, 주문자, 사이즈, 온도, 옵션, 비고, 금액, 생성·수정 시각을 저장한다.

## 사용자 기능

- 카테고리별 메뉴 선택과 직접 입력 메뉴를 지원한다.
- 온도는 ICE/HOT, 사이즈는 Tall/Grande를 지원한다.
- ICE 전용 메뉴와 Grande 미지원 메뉴는 선택을 제한한다.
- 주문자는 1인 1주문을 기본으로 하며 추가·수정·삭제할 수 있다.
- 주문 명세서는 동일 메뉴·사이즈·온도·옵션 조합을 묶어 수량과 총액을 계산한다.
- 주문 목록은 접힘 상태에서도 총 주문 수를 표시한다.
- 진행 중인 시즌 메뉴는 외부 아띠제 새메뉴 API에서 조회한다.

## 관리자 기능

- 인증된 관리자만 팀원, 전체 음료 목록, 주문 마감/재개, 전체 초기화를 변경할 수 있다.
- 옵션은 주문 화면과 관리자 화면에서 추가할 수 있다.
- 주문 마감 상태에서는 주문 추가·수정·삭제·초기화를 서버와 화면 양쪽에서 차단한다.
- 관리자 비밀번호 원문은 브라우저용 설정 파일로 제공하지 않는다.

## 동기화

- 화면은 최초 진입 시 전체 상태를 가져온다.
- 이후 5초마다 주문과 설정을 재조회한다.
- 저장 요청이 성공하면 현재 화면은 즉시 갱신하고, 다른 화면은 다음 동기화 주기에 갱신한다.

## 배포

- `.openai/hosting.json`에 `"d1": "DB"`를 선언한다.
- `drizzle/0000_sites_d1.sql`이 테이블과 초기 데이터를 생성한다.
- 저장된 Sites 버전을 배포해야 D1 마이그레이션과 API가 적용된다.
