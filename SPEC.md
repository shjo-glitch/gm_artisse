# 아띠제 음료 주문표 기능 명세서

## 개요

`아띠제 음료 주문표`는 회사 내부 구성원의 음료 주문을 한 화면에서 수집하고, 서버와 실시간 동기화해 함께 확인하는 웹 앱이다.

아티제 공식 음료 페이지의 카테고리 구조를 참고해 음료를 `커피`, `과일음료`, `차`, `기타`로 분류한다. 2026-06-04 확인 기준 공식 음료 페이지는 Gatsby 앱으로 렌더링되지만 항목 단위 음료명/가격 데이터는 공개 렌더링 결과에 노출되지 않고 카테고리 설명만 제공한다. 따라서 앱은 서버 관리자가 편집 가능한 내부 음료/가격 배열을 사용한다.

## 프로젝트 목표

- 음료 주문을 주문자, 메뉴, 사이즈, 옵션, 비고 단위로 명확히 기록한다.
- 카테고리별 음료 드롭다운과 직접 입력 메뉴를 모두 지원한다.
- Tall/Venti 가격을 기준으로 주문별 금액과 전체 총액을 계산한다.
- 시즌 메뉴 변동에 대응할 수 있도록 서버 관리자용 음료 관리 페이지를 제공한다.
- 여러 접속자가 같은 주문 상태를 볼 수 있도록 REST API와 SSE 동기화를 제공한다.

## 기능 명세

### 1. 사용자/사이즈/옵션

- 사용자는 하드코딩하지 않는다. 최초 팀원 명단은 0명이며, 로컬 관리자 페이지에서 등록한 팀원만 주문자로 선택할 수 있다. 팀원 목록은 `data/users.json`에 저장된다.
- 사이즈는 `Tall`, `Venti` 두 가지이다.
- 옵션은 기본값 `연하게`, `덜달게`, `디카페인`, `더달게`, `두유`, `오트밀크`를 제공하고, 주문 화면의 `+ 추가` 또는 관리자 페이지에서 서버 옵션 배열로 확장할 수 있다. 옵션 목록은 `data/options.json`에 저장된다.

### 2. 음료 카테고리와 가격

- 카테고리는 공식 음료 페이지를 참고해 다음 key로 관리한다.
  - `coffee`: 커피
  - `real-fruit-beverage`: 과일음료
  - `tea`: 차
  - `others`: 기타
- 음료 배열은 `{ category, name, prices: { Tall, Venti } }` 형태이다.
- 서버는 매 1시간마다 `cafeartisee.com` 페이지 번들에서 음료 메뉴를 자동 파싱하여 총 27개 기본 음료를 로드한다. 음료 배열은 `data/drinks.json`에 저장된다.
- 서버 관리자는 로컬 접속(`localhost`, `127.0.0.1`, `::1`)에서만 `admin.html` 관리자 페이지로 팀원 명단, 옵션 목록, 음료 배열을 조회, 추가, 수정, 저장할 수 있다. 팀원 명단은 최초 0명으로 시작하며 관리자가 직접 등록한다.
- 음료 배열 저장 시 중복 항목은 카테고리와 이름 기준으로 정리한다.

### 3. 주문 폼

- 주문 폼 첫 줄(row 1)에는 메뉴 이름, 온도, 주문자, 사이즈가 나란히 배치된다.
- 메뉴 이름은 드롭다운 선택 또는 직접 입력을 지원한다.
- 드롭다운은 카테고리별 그룹으로 표시하되, 각 항목 라벨에는 메뉴 이름만 표시한다.
- 온도는 `ICE`, `HOT`에서 선택하며, 기본값은 `ICE`이다.
- 직접 입력 메뉴의 메뉴명과 가격 필드는 row 2~3에 배치된다.
- 직접 입력 메뉴는 총액 계산을 위해 가격 입력 필드를 함께 사용한다.
- 주문자는 관리자가 등록한 팀원 목록에서 선택하며, 이미 주문한 사람은 드롭다운에서 제외해 중복 주문을 막는다 (1인 1주문). 주문 수정 중에는 해당 주문자만 예외로 다시 선택지에 포함한다.
- 사이즈는 `Tall`, `Venti`에서 선택한다.
- 옵션은 복수 선택 가능하며, 체크박스가 보이지 않는 토글 버튼 형태로 제공한다. `+ 추가`로 새 옵션을 입력하면 서버의 전체 옵션 배열에 즉시 저장되고 SSE로 동기화된다.
- 비고에는 추가 요청사항을 입력한다.

### 4. 주문 목록과 명세서

- 주문 명세서 카드는 주문 폼 위에 위치하며, 모든 주문을 메뉴명, 사이즈, 옵션 조합 기준으로 그룹핑한다.
- 주문 명세서 항목은 `메뉴 x수량 --- 총 금액` 형식으로 표시한다.
- 주문 명세서 카드에서 전체 주문의 총액을 표시한다.
- 주문 목록 카드는 주문 명세서 아래에 위치하며, 기본 상태는 접혀있고 `펼치기/접기` 버튼으로 토글 가능하다.
- 펼친 상태에서 각 사용자의 개별 주문을 메뉴명, 카테고리, 주문자, 사이즈, 온도, 옵션, 비고와 함께 표시한다. 주문별 가격은 표시하지 않는다.
- 선택된 음료의 가격은 사이즈별 가격 배열에서 계산하되 가격은 주문 명세서에서만 표시한다.
- 직접 입력 메뉴는 사용자가 입력한 가격으로 계산한다.
- 주문은 추가, 수정, 삭제, 전체 초기화를 지원한다.
- 주문 기록은 `data/orders.json`에 저장된다.

### 5. 진행중인 시즌메뉴 안내

- 주문 추가 영역 상단에는 `진행중인 시즌메뉴` 영역을 표시한다.
- 서버는 `cafeartisee.apis.flyground.co.kr/new-menu` API에서 각 항목의 제목과 행사기간을 가져온다.
- API는 전체 145개 게시물을 한 번에 반환하며, 페이지네이션을 지원하지 않는다.
- API 응답의 각 아이템 필드: `id`, `title`, `release_date`, `expire_date`, `thumb_image`, `detail_image`, `forReal`, `forAlpha` — 카테고리 필드는 없다.
- 음료 필터링은 제목에 `음료` 텍스트 키워드가 포함되었는지 여부로만 판별한다.
- 조건: 제목에 `음료` 키워드가 포함되고 현재 날짜가 판매 기간 내인 항목만 표시한다.
- API 조회 루틴은 서버 구동 시 1회 실행하고, 서버 구동 후 30분마다 자동 갱신한다. 최초 조회 시 아직 갱신이 완료되지 않았으면 `GET /api/season-menu`는 갱신 완료를 기다린 뒤 결과를 반환한다.
- 음료 키워드가 포함된 행사가 있으면 항목명, 기간, 공식 새메뉴 페이지 링크를 표시한다. 로딩 중에는 로딩 문구를 표시하고, 로딩 완료 후 조건에 맞는 항목이 없을 때만 없음 안내와 공식 새메뉴 페이지 링크를 표시한다.
- 신메뉴 주문은 직접 입력 메뉴명과 가격 입력으로 처리한다.

### 6. 서버 API

- `GET /api/config`: 사용자, 사이즈, 옵션, 카테고리를 반환한다.
- `GET /api/users`, `PUT /api/users`: 팀원 명단을 조회/교체한다. 기본 팀원은 없으며 저장된 팀원은 가나다 순으로 정렬된다. 빈 문자열 또는 빈 배열 저장으로 팀원 명단을 0명으로 비울 수 있다.
- `GET /api/options`, `POST /api/options`, `PUT /api/options`: 옵션 목록을 조회, 단건 추가, 전체 교체한다.
- `GET /api/drinks`: 음료 배열을 반환한다.
- `PUT /api/drinks`: 음료 배열을 전체 교체한다.
- `GET /api/season-menu`: 마지막 스크래핑 시각, 공식 새메뉴 URL, 진행 중인 음료 시즌메뉴 배열을 반환한다.
- `GET /api/orders`: 주문 목록과 총액을 반환한다.
- `POST /api/orders`: 주문을 추가한다.
- `PUT /api/orders/:id`: 주문을 수정한다.
- `DELETE /api/orders/:id`: 주문을 삭제한다.
- `POST /api/orders/reset`: 주문 목록을 초기화한다.
- `GET /api/events`: SSE `app-state` 이벤트로 최신 상태를 브로드캐스트한다.

### 7. 데이터 영속화

- 주문, 음료, 팀원 목록, 옵션은 `data/` 폴더의 JSON 파일에 저장된다.
  - `data/orders.json`: 주문 기록
  - `data/drinks.json`: 음료 메뉴
  - `data/users.json`: 팀원 목록
  - `data/options.json`: 옵션 목록
- `data/` 폴더는 `.gitignore`에 포함되어 개인정보를 보호한다.
- 서버 재시작 시 `data/` 폴더에서 이전 데이터를 복구한다. 폴더가 없으면 기본값으로 초기화한다.
- 외부에서 `data/drinks.json`, `data/users.json`, `data/options.json` 파일을 직접 수정하면 서버가 파일 변경을 자동 감지하여 메모리에 반영하고 SSE로 모든 접속자에게 브로드캐스트한다.
- `data/orders.json`은 자동 감지 대상에서 제외된다 (주문은 직접 수정이 의도된 동작이 아님).
- 서버가 파일을 직접 쓸 때는 감지 메커니즘을 무시하기 위해 내부 락을 사용한다.
- 다중 서버 인스턴스 간 상태 공유는 지원하지 않는다.

## 동작 시나리오

### 시나리오 1. 카테고리별 메뉴 선택 주문

1. 관리자가 먼저 로컬호스트에서 `admin.html`을 열어 팀원을 등록한다. (예: 홍길동|김철수|이영희)
2. 사용자가 `index.html`(주문표)을 연다.
3. 메뉴 드롭다운에서 `커피` 카테고리의 `카페 아메리카노`를 선택한다.
4. 온도 드롭다운에서 `ICE`를 선택한다. (기본값)
5. 주문자 드롭다운에서 `홍길동`, 사이즈 `Tall`, 옵션 `덜달게`를 선택한다.
6. 주문을 추가하면 주문 목록에 온도, 옵션이 표시되고 가격은 표시되지 않는다.
7. 주문 명세서 카드에 `카페 아메리카노 Tall (덜달게) x1 --- 4,900원` 형식으로 반영된다.

### 시나리오 2. 직접 입력 메뉴 주문

1. 사용자가 메뉴 이름에서 `직접 입력`을 선택한다.
2. 메뉴명과 가격을 입력한다.
3. 주문자, 사이즈, 옵션, 비고를 입력하고 주문을 추가한다.
4. 입력한 가격이 총액에 반영된다.

### 시나리오 3. 주문 명세서 종합

1. 사용자 A와 B가 같은 메뉴, 같은 사이즈, 같은 옵션으로 주문한다.
2. 주문 목록에는 각 사용자의 주문이 개별 표시된다.
3. 주문 명세서에는 동일 조합이 하나로 묶여 `x2` 수량으로 표시된다.
4. 해당 항목의 총 금액은 두 주문 가격의 합으로 표시된다.

### 시나리오 4. 서버 관리자의 시즌 메뉴 변경

1. 서버 관리자가 서버 장비의 로컬 주소로 `admin.html`을 연다.
2. 카테고리, 음료 이름, Tall/Venti 가격을 입력해 새 음료를 추가한다.
3. 저장 버튼으로 `PUT /api/drinks`에 음료 배열을 전송한다.
4. 주문표의 드롭다운은 SSE 또는 재로드를 통해 최신 음료 배열을 반영한다.

### 시나리오 5. 여러 사용자 동기화

1. 사용자 A가 주문을 추가한다.
2. 서버가 주문 목록과 총액을 갱신한다.
3. 서버는 `app-state` 이벤트로 모든 접속자에게 최신 상태를 전송한다.
4. 사용자 B 화면의 주문 목록과 총액이 갱신된다.
