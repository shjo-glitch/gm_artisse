# 아띠제 음료 주문표 기능 명세서

## 개요

`아띠제 음료 주문표`는 회사 내부 구성원의 음료 주문을 한 화면에서 수집하고, 실시간으로 동기화해 함께 확인하는 웹 앱이다.

별도의 애플리케이션 서버를 두지 않고, 정적 프론트엔드(HTML/CSS/JS)가 **Supabase**(Postgres + Realtime)를 직접 사용한다. 배포는 GitHub Pages로 한다. 아티제 공식 음료 페이지의 카테고리 구조를 참고해 음료를 `커피`, `과일음료`, `차`, `기타`로 분류하며, 음료/가격 데이터는 관리자가 편집한다.

## 프로젝트 목표

- 음료 주문을 주문자, 메뉴, 온도, 사이즈, 옵션, 비고 단위로 명확히 기록한다.
- 카테고리별 음료 드롭다운과 직접 입력 메뉴를 모두 지원한다.
- Tall/Grande 가격을 기준으로 주문별 금액과 전체 총액을 계산한다.
- 관리자가 팀원·옵션·음료를 관리하고, 필요 시 주문을 마감/재개할 수 있다.
- 여러 접속자가 같은 주문 상태를 보도록 Supabase Realtime으로 동기화한다.

## 아키텍처

- 프론트엔드는 `@supabase/supabase-js`(CDN)로 Supabase에 직접 접근한다.
- 환경 설정은 `supabase-config.js`의 전역 변수 `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `ADMIN_PASSWORD`로 주입한다. 이 파일은 커밋하지 않으며 배포 시 GitHub Actions가 시크릿으로 생성한다.
- RLS는 내부 도구 특성상 anon 키 전체 허용이며, 관리자 게이트는 `ADMIN_PASSWORD` 기반 클라이언트 측 검증(`sessionStorage`)이다.

## 데이터 모델

- `config` (단일 행, id=1)
  - `users text[]` — 팀원 명단 (가나다 순)
  - `options text[]` — 옵션 목록 (기본 6종)
  - `orders_closed boolean` — 주문 마감 여부
- `drinks` — `category`, `name`, `tall_price`, `grande_price`, `ice_only`, `sort_order` (PK: category+name)
- `orders` — `id`, `menu_name`, `category`, `requester`, `size`(`Tall`|`Grande`), `temp`(`ICE`|`HOT`), `options text[]`, `note`, `unit_price`, `created_at`, `updated_at`

## 기능 명세

### 1. 사용자/사이즈/옵션
- 사용자는 하드코딩하지 않는다. 최초 팀원 명단은 0명이며, 관리자가 등록한 팀원만 주문자로 선택할 수 있다. (`config.users`)
- 사이즈는 `Tall`, `Grande` 두 가지이다. (DB 컬럼: `tall_price`, `grande_price`)
- 옵션은 기본값 `연하게`, `덜달게`, `디카페인`, `더달게`, `두유`, `오트밀크`를 제공하고, 주문 화면의 `+ 추가` 또는 관리자 페이지에서 확장한다. (`config.options`)

### 2. 음료 카테고리와 가격
- 카테고리 key: `coffee`(커피), `real-fruit-beverage`(과일음료), `tea`(차), `others`(기타).
- 음료는 `{ category, name, prices: { Tall, Grande }, iceOnly }` 형태로 다룬다. `prices.Grande === 0`이면 Grande가 없는 메뉴이며, `iceOnly`이면 HOT을 선택할 수 없다.
- 관리자는 `admin.html`에서 음료 배열을 조회·추가·수정·저장한다. 저장 시 카테고리+이름 기준으로 중복을 정리한다.
- 기본 음료 27종이 프론트엔드에 폴백으로 내장되어, DB가 비어 있을 때 사용된다.

### 3. 주문 폼
- 첫 줄에 메뉴 이름, 온도, 주문자, 사이즈가 나란히 배치된다.
- 메뉴 이름은 드롭다운 선택(카테고리 그룹) 또는 직접 입력을 지원한다. 직접 입력 시 메뉴명·가격 필드가 나타난다.
- 온도는 `ICE`, `HOT`이며 기본값은 `ICE`. ICE only 음료는 HOT이 비활성된다.
- 주문자는 등록된 팀원에서 선택하며, 이미 주문한 사람은 제외해 중복을 막는다(1인 1주문). 수정 중에는 해당 주문자만 예외로 포함한다.
- 사이즈는 `Tall`, `Grande`. Grande 가격이 0인 메뉴는 Grande가 비활성된다.
- 옵션은 복수 선택 가능하며 토글 버튼 형태로 제공한다. `+ 추가`로 새 옵션을 입력하면 `config.options`에 즉시 저장되고 Realtime으로 동기화된다.
- 비고에 추가 요청사항을 입력한다.

### 4. 주문 목록과 명세서
- 주문 명세서 카드는 모든 주문을 메뉴명·사이즈·온도·옵션 조합 기준으로 그룹핑하고 `메뉴 온도 사이즈(옵션) x수량 --- 총 금액` 형식으로 표시하며 전체 총액을 보여준다.
- 주문 목록 카드는 기본 접힘 상태이며 펼치면 각 주문을 메뉴명·주문자·온도·사이즈·카테고리·옵션·비고와 함께 표시한다. 개별 주문 가격은 표시하지 않는다.
- 선택 음료의 가격은 사이즈별 가격으로 계산하고, 직접 입력 메뉴는 입력 가격으로 계산한다.
- 주문은 추가·수정·삭제·전체 초기화를 지원한다. 전체 초기화는 관리자 전용이다.

### 5. 주문 마감 / 재개 (관리자 전용)
- 관리자는 주문표에서 **주문 마감/재개**를 토글할 수 있다. 상태는 `config.orders_closed`에 저장된다.
- 마감 상태(`orders_closed = true`)에서는 모든 사용자에 대해 주문 추가·수정·삭제·전체 초기화·옵션 추가가 차단되고, 주문 폼 상단에 마감 안내 배너가 표시된다.
- 재개하면 다시 주문할 수 있다.
- 마감/재개는 Realtime `config` 변경으로 전 접속자에게 즉시 반영되며, 변경 시 토스트로 안내한다.
- 마감 토글 자체는 관리자만 수행할 수 있다(클라이언트 검증). 마감/재개 버튼은 관리자 인증 시에만 노출된다.

### 6. 진행중인 시즌메뉴 안내
- 주문 폼 상단에 `진행중인 시즌메뉴` 영역을 표시한다.
- 프론트엔드가 `cafeartisee.apis.flyground.co.kr/new-menu`를 직접 조회한다. (페이지네이션 없음)
- 제목에 `음료` 키워드가 포함되고 현재 날짜가 판매 기간(`release_date`~`expire_date`) 내인 항목만 표시한다.
- 로딩 중에는 로딩 문구를, 조건에 맞는 항목이 없으면 없음 안내와 공식 새메뉴 페이지 링크를 표시한다.
- 신메뉴 주문은 직접 입력 메뉴명·가격으로 처리한다.

### 7. 실시간 동기화
- Supabase Realtime으로 `orders`, `drinks`, `config` 테이블 변경을 구독한다.
- 변경 발생 시 해당 데이터를 재조회하여 모든 접속자 화면(주문 목록·총액·메뉴·팀원·옵션·마감 상태)을 갱신한다.
- 다중 Supabase 인스턴스 간 상태 공유는 고려하지 않는다(단일 프로젝트 기준).

## 동작 시나리오

### 시나리오 1. 카테고리별 메뉴 선택 주문
1. 관리자가 `admin.html`에서 팀원을 등록한다. (예: 홍길동|김철수|이영희)
2. 사용자가 `index.html`을 연다.
3. 메뉴 드롭다운에서 `커피`의 `카페 아메리카노`를 선택한다.
4. 온도 `ICE`(기본), 주문자 `홍길동`, 사이즈 `Tall`, 옵션 `덜달게`를 선택한다.
5. 주문을 추가하면 주문 목록에 온도·옵션이 표시되고 가격은 표시되지 않는다.
6. 주문 명세서에 `카페 아메리카노 ICE T (덜달게) x1 --- 4,900원` 형식으로 반영된다.

### 시나리오 2. 직접 입력 메뉴 주문
1. 메뉴에서 `직접 입력`을 선택한다.
2. 메뉴명과 가격을 입력한다.
3. 주문자·사이즈·옵션·비고를 입력하고 추가하면 입력 가격이 총액에 반영된다.

### 시나리오 3. 주문 명세서 종합
1. 사용자 A·B가 같은 메뉴·사이즈·온도·옵션으로 주문한다.
2. 주문 목록에는 개별 표시되고, 명세서에는 하나로 묶여 `x2`로 표시된다.
3. 해당 항목 총액은 두 주문 가격의 합이다.

### 시나리오 4. 주문 마감/재개
1. 관리자가 주문표에서 **주문 마감**을 누른다.
2. `config.orders_closed`가 `true`로 바뀌고 Realtime으로 전 접속자에게 전파된다.
3. 모든 사용자 화면에 마감 배너가 뜨고 주문 폼·수정·삭제·초기화가 차단된다.
4. 관리자가 **주문 재개**를 누르면 다시 주문할 수 있다.

### 시나리오 5. 여러 사용자 동기화
1. 사용자 A가 주문을 추가한다.
2. Supabase가 `orders` 변경 이벤트를 broadcast한다.
3. 사용자 B 화면의 주문 목록과 총액이 자동 갱신된다.
